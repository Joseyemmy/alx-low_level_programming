#include "main.h"

/**
 * puts2 - prints every other char of the string
 * @str: input string to input
 */

void puts2(char *str)
{
	int i;

	while (str[i] != '\0')
	{
		if (i % 2 == 0)
			_putchar;

			(tr[i]);

			i++;
	}

	_putchar('\n')
}
